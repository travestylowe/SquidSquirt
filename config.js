// Leave empty for localStorage-only global counter; set both for Supabase (see README).
window.SQUIDSQUIRT_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  /** Log fetch/audio issues to the console (development). */
  debug: false,
};
